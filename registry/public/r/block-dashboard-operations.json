{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "block-dashboard-operations",
  "title": "Block · Dashboard · Operations",
  "description": "Astrale block/dashboard/operations; controlled application responsibilities are injected through props.",
  "dependencies": [
    "@astrale-os/ui@^0.3.0-beta.0"
  ],
  "files": [
    {
      "path": "registry/blocks/dashboard/operations.tsx",
      "content": "import { Button } from '@astrale-os/ui/button'\nimport { Card, CardContent, CardHeader, CardTitle } from '@astrale-os/ui/card'\nexport function DashboardOperations({\n  className,\n  style,\n  metrics,\n  loading,\n  error,\n  onRefresh,\n  onMetric,\n}: {\n  className?: string\n  style?: React.CSSProperties\n\n  metrics: readonly { id: string; label: string; value: React.ReactNode; detail?: string }[]\n  loading?: boolean\n  error?: string\n  onRefresh(): void\n  onMetric(id: string): void\n}) {\n  return (\n    <section\n      data-slot=\"block-dashboard-operations\"\n      style={style}\n      aria-busy={loading}\n      className={className}\n    >\n      <header\n        data-slot=\"blocks-dashboard-operations-header\"\n        className=\"flex items-center justify-between\"\n      >\n        <h1 data-slot=\"blocks-dashboard-operations-h1\" className=\"font-heading text-3xl\">\n          Operations\n        </h1>\n        <Button variant=\"outline\" onClick={onRefresh} disabled={loading}>\n          Refresh\n        </Button>\n      </header>\n      {error && (\n        <p data-slot=\"blocks-dashboard-operations-p\" role=\"alert\">\n          {error}\n        </p>\n      )}\n      <div data-slot=\"blocks-dashboard-operations-div\" className=\"mt-5 grid gap-4 md:grid-cols-3\">\n        {metrics.map((metric) => (\n          <Card key={metric.id}>\n            <CardHeader>\n              <CardTitle className=\"text-sm\">{metric.label}</CardTitle>\n            </CardHeader>\n            <CardContent>\n              <Button\n                variant=\"ghost\"\n                className=\"h-auto p-0 text-3xl\"\n                onClick={() => onMetric(metric.id)}\n              >\n                {metric.value}\n              </Button>\n              {metric.detail && (\n                <p data-slot=\"blocks-dashboard-operations-p\" className=\"text-muted-foreground\">\n                  {metric.detail}\n                </p>\n              )}\n            </CardContent>\n          </Card>\n        ))}\n      </div>\n    </section>\n  )\n}\n",
      "type": "registry:component",
      "target": "components/astrale/components/dashboard/operations.tsx"
    }
  ],
  "meta": {
    "canonicalAddress": "block/dashboard/operations",
    "ownership": "consumer-source"
  },
  "type": "registry:block"
}