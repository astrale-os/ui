{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "component-checkbox-checkbox-06",
  "title": "Checkbox 6",
  "description": "Badge-style checkboxes with hidden unchecked state for compact selection",
  "dependencies": [
    "@astrale-os/ui@^0.3.0-beta.0",
    "react@19.2.8"
  ],
  "files": [
    {
      "path": "source/components/checkbox/checkbox-06/checkbox-06.tsx",
      "content": "'use client'\n\nimport { useState } from 'react'\n\nimport { Badge } from '@astrale-os/ui/badge'\nimport { Checkbox } from '@astrale-os/ui/checkbox'\n\nconst snacks = ['Burger', 'Pizza', 'Drinks']\n\nconst CheckboxBadgeDemo = () => {\n  const [selected, setSelected] = useState<string[]>(['Burger', 'Pizza'])\n\n  return (\n    <div className='flex items-center gap-2'>\n      {snacks.map(label => (\n        <Badge key={label} variant='secondary' className='relative h-auto gap-2 rounded-sm px-3 py-1.5'>\n          <Checkbox\n            id={label}\n            checked={selected.includes(label)}\n            onCheckedChange={checked =>\n              setSelected(checked ? [...selected, label] : selected.filter(item => item !== label))\n            }\n            className='data-unchecked:hidden'\n          />\n          <label htmlFor={label} className='cursor-pointer select-none after:absolute after:inset-0'>\n            {label}\n          </label>\n        </Badge>\n      ))}\n    </div>\n  )\n}\n\nexport default CheckboxBadgeDemo\n",
      "type": "registry:component",
      "target": "components/astrale/components/checkbox/checkbox-06.tsx"
    }
  ],
  "meta": {
    "canonicalAddress": "component/checkbox/checkbox-06",
    "provider": "@astrale-os/ui",
    "source": "@astrale-os/ui",
    "license": "MIT"
  },
  "type": "registry:component"
}